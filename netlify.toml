[build]
  command   = "npm run build"
  publish   = "dist"

# .html extension redirects (Webflow export compatibility)
[[redirects]]
  from   = "/*.html"
  to     = "/:splat"
  status = 301

[[redirects]]
  from   = "/index"
  to     = "/"
  status = 301
